import template from './add-plan.html';
import controller from './add-plan.controller';

let addPlanComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default addPlanComponent;
