import template from './new-event.html';
import controller from './new-event.controller';
import './new-event.less';

let newEventComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default newEventComponent;
